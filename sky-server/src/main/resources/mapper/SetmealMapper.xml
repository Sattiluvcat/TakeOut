<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">

    <insert id="insert" keyProperty="id" useGeneratedKeys="true" parameterType="Setmeal">
        insert into setmeal (category_id, name, price, description, image, status, create_time, update_time,
                             create_user, update_user)
        VALUES (#{categoryId},#{name},#{price},#{description},#{image},#{status},
                #{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>
    <update id="update">
        update setmeal
        <set>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="image != null">
                image = #{image},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="createUser != null">
                create_user= #{createUser}
            </if>
        </set>
        where id = #{id}
    </update>
    <select id="getSetmealPage" resultType="com.sky.vo.SetmealVO">
        select setmeal.*,category.name as categoryName
        from setmeal left outer join category on setmeal.category_id = category.id
        <where>
            <if test="name!=null">
                and setmeal.name like concat('%',#{name},'%')
            </if>
            <if test="status!=null">
                and setmeal.status=#{status}
            </if>
            <if test="categoryId!=null">
                and setmeal.category_id=#{categoryId}
            </if>
        </where>
        order by update_time desc
    </select>
</mapper>
